<Window x:Class="ArendWpfApp.ClientsShowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArendWpfApp"
        mc:Ignorable="d"
        Title="ClientsShowWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Width" Value="100"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button x:Name="BackButton" Content="Назад" Click="BackButton_Click"/>
        </StackPanel>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid x:Name="ShowDataGrid" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя клиента" Binding="{Binding Imya}"/>
                    <DataGridTextColumn Header="Фамилия клиента" Binding="{Binding Familya}"/>
                    <DataGridTextColumn Header="Отчество клиента" Binding="{Binding Otchestvo}"/>
                    <DataGridTextColumn Header="Серия паспорта" Binding="{Binding PasportSeria}"/>
                    <DataGridTextColumn Header="Номер паспорта" Binding="{Binding PasportNumber}"/>
                    <DataGridTextColumn Header="НомерВУ" Binding="{Binding NomerVU}"/>
                    <DataGridTextColumn Header="Контактный телефон" Binding="{Binding KontaktniiTelephon}"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1">
                <Button Content="Добавить" x:Name="AddButton" Click="AddButton_Click"/>
                <Button Content="Изменить" x:Name="EditButton" Click="EditButton_Click"/>
                <Button Content="Удалить" x:Name="DeleteButton" Click="DeleteButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
